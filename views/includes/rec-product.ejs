<div>
<% recprods.forEach(p => { %>           
    <h4><%= p.title %></h4>
    <img src="<%= p.image %>" alt="<%= p.title %>">
    <a href="/products/<%= p._id %>" class="btn">Details</a>
<% }) %>

</div>